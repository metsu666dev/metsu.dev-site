"use client"

/**
 * Custom cursor effects removed.
 *
 * Requirements:
 * - No DOM node injection / removeChild hacks
 * - Keep default system cursor
 * - Accent ring only on interactive elements (handled in CSS in app/globals.css)
 */
export function CursorGlow() {
  return null
}
